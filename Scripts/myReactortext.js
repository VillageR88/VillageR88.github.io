/*const text1 = "This is not a test.";
const text2 = "This is reality.";
const text3 = "World wide.";
*/
/*
myReactbar.style.transition = 'none';
myReactbar2.style.transition = 'none';
import toggleReactbar from "./reactbar.js"
toggleReactbar();

function sleep(ms) {return new Promise(resolve => setTimeout(resolve, ms))};
const element = document.getElementById('myReactortext');
async function typeText(text, element, delay) {
    for (var i = 0; i < text.length; i++) {
        element.textContent += text[i];
        await sleep(delay);
    }
}

async function blinking(rest, delay) {
  for (var i = 0; i < rest; i++) {
    element.textContent += "_";
    await sleep(delay);
    element.textContent = element.textContent.slice(0, -1);
    await sleep(delay);
  }
}

var go = 80;
var rest = 530;
go = 0, rest = 0;
async function main() {
    element.textContent += "Jeff-Bot: ";
    await typeText("Hello there!", element, go);
    await blinking(4, rest);
    element.textContent += "\n";
    element.textContent += "Jeff-Bot: ";
    await typeText("My name is Jeff", element, go);
    await blinking(1, rest);
    await typeText(" and I'm automated bot.", element, go);
    await blinking(4, rest);
    element.textContent += "\n";
    element.textContent += "Jeff-Bot: ";
    await typeText("I was designed to guide you safely through the process of activating React.", element, go);
    await blinking(4, rest);
    element.textContent += "\n";
    element.textContent += "Jeff-Bot: ";
    await typeText("No worries! The entire process is 100% safe and fully automated.", element, go);
    await blinking(4, rest);
    element.textContent += "\n";
    element.textContent += "Jeff-Bot: ";
    await typeText("Running the React startup routine.", element, go);
    await blinking(4, rest);
    element.textContent = "";
  }
  
  main();
  */